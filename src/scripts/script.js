function setup(){
    createCanvas(window.innerWidth, window.innerHeight);

}

function draw(){



    /* Draw in the canvas like a pencil
    if(mouseIsPressed){
        fill(0);
        circle(mouseX, mouseY, 5);
    }else{
        fill(100);
    } */

}